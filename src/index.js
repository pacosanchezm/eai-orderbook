import React from "react";
import ReactDOM from "react-dom";

import App from "../eai-orderbook/appi";

const rootElement = document.getElementById("root");
ReactDOM.render(

    <App 
      id={4575}
      // opt={3}
    />
,
  rootElement
);
